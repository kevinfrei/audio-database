{
  "name": "audio-database",
  "version": "0.1.1",
  "description": "NodeJS Audio/Music database",
  "author": "Kevin Frei <kevinfrei@hotmail.com>",
  "license": "Unlicense",
  "private": false,
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://kevinfrei/audio-database.git"
  },
  "scripts": {
    "test": "jest --config jestconfig.json",
    "watch": "jest --config jestconfig.json --watch",
    "build": "rimraf lib && tsc && minify -i lib",
    "prepare": "yarn run build",
    "prepublishOnly": "yarn test && yarn run lint",
    "format": "prettier --write \"src/**/*.ts\" \"*.md\" \"*.json\" \".vscode/*.json\"",
    "lint": "yarn run eslint --fix --ext .ts src",
    "preversion": "yarn run lint",
    "version": "yarn run format && git add -A src",
    "postversion": "git push && git push --tags --no-verify",
    "compile": "tsc --noEmit",
    "chk": "yarn lint && yarn compile && yarn test"
  },
  "dependencies": {
    "@freik/core-utils": "^0.24.0",
    "@freik/media-core": "^0.5.1",
    "@freik/media-utils": "^0.11.0",
    "@freik/node-utils": "^0.14.6",
    "xxhashjs": "^0.2.2"
  },
  "devDependencies": {
    "@freik/build-tools": "^2.7.2",
    "@types/jest": "^26.0.23",
    "@types/xxhashjs": "^0.2.2",
    "@typescript-eslint/eslint-plugin": "^4.26.0",
    "eslint": "^7.27.0",
    "husky": "^6.0.0",
    "jest": "^26.6.3",
    "pinst": "^2.1.6",
    "prettier": "^2.3.0",
    "ts-jest": "^26.5.6",
    "typescript": "^4.3.2"
  },
  "files": [
    "lib/**/*"
  ]
}
